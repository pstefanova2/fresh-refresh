// Function to check if the element with selector "#ember1127" exists on the page
/*
function checkForElement() {
    const element = document.querySelector("#ember1127");
    if (element) {
      console.log("Element found! Refreshing the page...");
      location.reload();
    } else {
      console.log("Element not found.");
    }
  }
  
  // Check for the element every 1 second
  setInterval(checkForElement, 1000);
  */